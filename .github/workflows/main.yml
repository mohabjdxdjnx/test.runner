jobs:
  build-on-mac:
    runs-on: macos-latest  # This triggers the Mac Cloud runner
    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Say Hello
        run: |
          sw_vers  # This prints the macOS version
          echo "Hello from a GitHub-hosted Mac!"
